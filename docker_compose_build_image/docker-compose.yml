# docker-compose.yml
version: '3'
services:
  python_service:
    build:
      context: .
      dockerfile: dockerfile.python
      args:
        - BASE_IMAGE=python
        - APP_VERSION=2.0
    environment:
      - DEBUG=true
    image: iranlinux/python_web:2.1

  node_service:
    build:
      context: .
      dockerfile: dockerfile.node
      args:
        - BASE_IMAGE=node
        - APP_NAME=my-nodejs-app
    environment:
      - NODE_ENV=development
    image: iranlinux/node_web:2.1
